import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Pesquisar"
}

const Search = () => {
    return (
        <div>
            Pesquisar
        </div>
    )
}

export default Search;