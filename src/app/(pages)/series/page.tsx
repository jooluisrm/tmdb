import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Séries"
}

const Series = () => {
    return (
        <div>
            SÉRIES
        </div>
    )
}

export default Series;