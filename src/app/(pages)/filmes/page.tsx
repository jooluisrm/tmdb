import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Filmes"
}

const Filmes = () => {
    return (
        <div>
            FILMES
        </div>
    )
}

export default Filmes;