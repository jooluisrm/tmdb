type Props = {
    params: {
        idTv: number;
        numTemp: number;
    }
}

export const SeasonMain = ({ params }: Props) => {
    return (
        <main className="container mx-auto">
            
        </main>
    );
}