import { Input } from "../reutilizados/input";

export const SearchMain = () => {
    return (
        <main className="container mx-auto">
            <div>
                <Input />
            </div>
        </main>
    );
}